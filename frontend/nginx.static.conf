# Internal container port only — the edge nginx (infra/nginx/nginx.conf)
# terminates TLS and adds security headers, then proxies here.
server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
